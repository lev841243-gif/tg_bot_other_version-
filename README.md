# 🤖 Telegram Bot для изучения английского языка

Telegram-бот для изучения английских слов с функционалом персонального словаря и интерактивными викторинами.

---

## 📋 Функциональность

- 🎯 **Изучение слов в формате викторины** — интерактивное обучение с выбором правильного ответа
- ➕ **Добавление собственных слов** — создайте персональный словарь под свои нужды 
- 🔙 **Удаление слов** — управляйте своим словарем
- ✅ **Счетчик изучаемых слов** - каждый пользователь видит свой прогресс в количестве изучаемых слов
- 📊 **База данных с разделением по пользователям** — каждый пользователь имеет свой набор слов
- 🏆 **10 предустановленных слов** — начните обучение сразу после запуска

---

## 🛠 Технологии

- **Python 3.11.5** ()
- **PostgreSQL** — надежное хранение данных
- **pyTelegramBotAPI** — работа с Telegram Bot API
- **psycopg2** — взаимодействие с PostgreSQL

---

## 📥 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://--------------------------- # Ваш репозиторий
cd Bot
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
# source .venv/bin/activate  # Linux/macOS
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `config.py` в корневой директории проекта:

```python
DB_CONFIG = {
    'dbname': 'english_bot_db',
    'user': 'postgres', 
    'password': 'ваш_пароль_postgresql',
    'host': 'localhost',
    'port': '5432'
}

BOT_TOKEN = 'ваш_токен_бота_от_BotFather'
```

> ⚠️ **Важно:** Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

### 5. Настройка базы данных PostgreSQL

#### Вариант 1: Через pgAdmin4

1. Откройте pgAdmin4
2. Подключитесь к серверу PostgreSQL
3. Создайте новую базу данных с именем `english_bot_db`

#### Вариант 2: Через SQL

```sql
CREATE DATABASE english_bot_db;
```

> ✅ **Примечание:** Таблицы создадутся автоматически при первом запуске бота!

### 6. Запуск бота

```bash
python main.py
```

### 7. Использование

В Telegram найдите вашего бота и отправьте команду `/start`

---

## 🗃 Структура базы данных

### Схема БД

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│   users     │    │  user_words  │    │    words    │
├─────────────┤    ├──────────────┤    ├─────────────┤
│ user_id (PK)│◄───│ user_id (FK) │    │ word_id (PK)│
│ username    │    │ word_id (FK) │───►│ english_word│
│ created_at  │    │ is_active    │    │ russian_tr  │
└─────────────┘    └──────────────┘    │ is_common   │
                                       └─────────────┘
```

### Описание таблиц

#### 1. Таблица `users`
Хранит информацию о пользователях бота.

| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | BIGINT PRIMARY KEY | ID пользователя в Telegram |
| `username` | VARCHAR(100) | Имя пользователя |
| `created_at` | TIMESTAMP | Дата регистрации |

#### 2. Таблица `words`
Хранит слова и их переводы.

| Поле | Тип | Описание |
|------|-----|----------|
| `word_id` | SERIAL PRIMARY KEY | Автоинкрементный ID слова |
| `english_word` | VARCHAR(100) NOT NULL | Слово на английском |
| `russian_translation` | VARCHAR(100) NOT NULL | Перевод на русский |
| `is_common` | BOOLEAN DEFAULT FALSE | Флаг общих слов (True - для всех пользователей) |

#### 3. Таблица `user_words`
Связывает пользователей с их словами (отношение многие-ко-многим).

| Поле | Тип | Описание |
|------|-----|----------|
| `user_word_id` | SERIAL PRIMARY KEY | Автоинкрементный ID связи |
| `user_id` | BIGINT FOREIGN KEY | Ссылка на `users.user_id` |
| `word_id` | INTEGER FOREIGN KEY | Ссылка на `words.word_id` |
| `is_active` | BOOLEAN DEFAULT TRUE | Флаг активности слова для пользователя |

---

## 🔧 Использование бота

### Команды

- `/start` — начать работу с ботом
- `/cards` — показать карточку для изучения

### Кнопки управления

- **Добавить слово ➕** — добавить новое слово в персональный словарь
- **Удалить слово 🔙** — удалить слово из персонального словаря
- **Дальше ⏭** — показать следующее слово

### Процесс обучения

1. Бот показывает русский перевод слова
2. Пользователь выбирает правильный английский вариант из 4 кнопок
3. При правильном ответе — подтверждение ✅
4. При ошибке — возможность повторить ❌
5. Можно добавлять/удалять слова для персонализации обучения

---

## 📊 Особенности реализации

- ✨ **Персональные словари** — каждый пользователь имеет свой набор слов
- 🌐 **Общие слова** — 10 предустановленных слов доступны всем пользователям
- 🎯 **Гибкое удаление** — удаление слов работает только для текущего пользователя
- 💾 **Состояние бота** — сохранение состояния между сообщениями

---

## 👥 Разработчик

[Name/Contact]

---

## 📝 Лицензия

[unlicensed]
